# Conservative release obfuscation for the first store build.
#
# Keep property, top-level, filename and export-name obfuscation disabled for now.
# This project uses object keys for RDB columns, JSON route params, service card data,
# and framework entry points, so aggressive name obfuscation needs dedicated device
# regression before being enabled.

-compact
-remove-log
